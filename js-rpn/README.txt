README.txt for js-rpn project 

To run tests from the command line: 

cd js-rpn                          project base directory 
mocha --reporter=nyan rpn.js       execute tests 

Add the current working directory to PATH

export PATH=".:$PATH"

Script to run mocha:

tests
