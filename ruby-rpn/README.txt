README.txt for ruby-rpn project 

To run tests from the command line: 

cd ruby-rpn               project base directory 
bundle exec rake          execute tests 

Add current working directory to PATH:

export PATH=".:$PATH"

Script to run tests: 

tests
